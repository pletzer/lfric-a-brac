name: lfricabrac

channels:
  - conda-forge

dependencies:
  - python>=3.8

# Setup dependencies.
  - setuptools
  - tbb-devel
  - numpy
  - pip

# Core dependencies.
  - scitools-iris
  - libnetcdf
  - vtk>=9.1.0
  - xarray
  - defopt
  - python-mint>=1.25.0

